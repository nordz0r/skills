interface:
  display_name: "Nextcloud Admin"
  short_description: "Nextcloud files and administration via WebDAV and OCS API: uploads, shares, users, groups, apps."
  default_prompt: "Use $nextcloud-admin to manage Nextcloud files, shares, users, and groups over WebDAV and the OCS API."
